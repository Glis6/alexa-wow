/**
 * The score for all specs together.
 */
export const all: string = "all";

/**
 * The score for just the tanking spec.
 */
export const tank: string = "tank";

/**
 * The score for just the DPS spec.
 */
export const dps: string = "dps";

/**
 * The score for just the healer spec.
 */
export const healer: string = "healer";